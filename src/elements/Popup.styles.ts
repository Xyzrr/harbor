import styled from 'styled-components';

export const Wrapper = styled.div`
  width: fit-content;
  height: fit-content;
`;

export const Shield = styled.div`
  position: fixed;
  top: 0;
  left: 0;
  width: 100vw;
  height: 100vw;
  z-index: 100;
`;
